<%
const {
  isNull = false
} = props || {}
%>

<div class="ui-guide-notepannel <%-isNull ? 'is-null' : ''%>">
  <%_if(isNull){_%>
  <!-- 창업노트 없을때:S -->
  <div class="pg-conts">
    <div class="g-null-area">
      <span class="ico">
        <img src="<%-$staticSrc('@/static/images/common/icon_note.svg')%>" class="img" alt="" />
      </span>
      <div class="g-pue">
        <div class="g-st">
          진행했던 과정을 기록하고<br />
          나만의 창업스토리를 만들어보세요!
        </div>
      </div>
    </div>
  </div>
  <div class="pg-bt-area">
    <%-include($rootSrc('@/components/BsButton'), {
      props:{
        block: true,
        size: (size)=>size.LARGE,
        cssBackground: (color)=>color['Common/White'],
        cssColor: (color)=>color['Gray/02'],
        cssBorderColor: (color)=>color['Gray/02'],
        slot: '기록하기'
      }
    })%>
  </div>
  <!-- 창업노트 없을때:E -->
  <%_}else{_%>
  <!-- 창업노트 있을때:S -->
  <div class="pg-conts">
    필요한 내용을 적어두고 확인하세요.
    필요한 내용을 적어두고 확인 필요한 내용을 적어두고 확인하세요.
    필요한 내용을 적어두고 확인하세요.

    입력한 내용만큼 영역이 늘어납니다.
  </div>
  <div class="pg-opcti">
    <!-- foreach:S -->
    <div class="po-tp">
      <button class="p-bt">
        <img src="<%-$staticSrc('@/static/images/@dummy/banner23.png')%>" class="img" alt="" />
      </button>
    </div>
    <div class="po-tp">
      <button class="p-bt">
        <img src="<%-$staticSrc('@/static/images/@dummy/banner23.png')%>" class="img" alt="" />
      </button>
    </div>
    <div class="po-tp">
      <button class="p-bt">
        <img src="<%-$staticSrc('@/static/images/@dummy/banner23.png')%>" class="img" alt="" />
      </button>
    </div>
    <div class="po-tp">
      <button class="p-bt">
        <img src="<%-$staticSrc('@/static/images/@dummy/banner23.png')%>" class="img" alt="" />
      </button>
    </div>
    <!-- foreach:E -->
  </div>
  <div class="pg-bt-area">
    <%-include($rootSrc('@/components/BsButton'), {
      props:{
        block: true,
        size: (size)=>size.LARGE,
        cssBackground: (color)=>color['Common/White'],
        cssColor: (color)=>color['Gray/02'],
        cssBorderColor: (color)=>color['Gray/02'],
        slot: '수정하기'
      }
    })%>
  </div>
  <!-- 창업노트 있을때:E -->
  <%_}_%>

</div>